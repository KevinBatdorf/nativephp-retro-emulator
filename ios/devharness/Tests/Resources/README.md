Test media staged here is never committed (repo media policy) — see the root
.gitignore. Drop `rdpqdemo.z64` here for the N64 render check; it skips when
absent. (No dotfiles: codesign rejects the copied resource bundle.)
