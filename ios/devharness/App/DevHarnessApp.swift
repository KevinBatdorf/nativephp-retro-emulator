import SwiftUI

// Empty shell — exists only to host DeviceTests on a physical device.
@main
struct DevHarnessApp: App {
    var body: some Scene {
        WindowGroup {
            Text("RetroEmulator device test harness")
        }
    }
}
