/vendor/
/node_modules/

# Test ROMs — fetched via scripts/fetch_test_roms.sh, never committed
/tests/roms/

# Prebuilt native libs are release assets now (resources/native-assets.json),
# not tracked files. Build locally via scripts/build_android_libs.sh.
/resources/android/jniLibs/
.DS_Store
*.log

# Private local working files — never tracked
.claude/

# Dev-supplied firmware + commercial ROMs — banned from the repo by policy,
# kept local-only for on-device verification
/local/

# Android
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/app/.cxx/
android/.idea/
android/*.iml

# iOS / native build output
/build/
.build/
.swiftpm/
# Dev-harness only: prebuilt librashader copied from resources/ by
# scripts/build_librashader_android.sh (the committed source of truth).
android/app/src/main/jniLibs/

# iOS test media staged locally, never committed
ios/test/Tests/EmulatorCoreTests/TestMedia/*
ios/devharness/Tests/Resources/*
!ios/test/Tests/EmulatorCoreTests/TestMedia/README.md
!ios/devharness/Tests/Resources/README.md

# Engine source clones for the bundled fast-core builds (never vendored wholesale)
/vendor-engines/

# BYO libretro cores staged for instrumented tests — never committed
android/app/src/androidTest/jniLibs/
# BYO cores staged for dev-harness measurement runs — never committed
android/app/src/main/jniLibs/
.phpunit.cache/
